
int main() {
    // Declare variables and initialize them to some values
    int x = 5;
    float y = 3.14;
    char z = 'a';

    // Print the values of the variables
    printf("x: %d\ny: %f\nz: %c\n", x, y, z);

    return 0;
}