#include <stdio.h>

int main() {
    int age = 20;
    if (age >= 18) {
        printf("You are eligible to vote.\n");
    } else {
        printf("Sorry, you are not eligible to vote yet.\n");
    }
    return 0;
}
